#include "main.h"

/**
 * flag_check - used to check all the flag elements in the specifiers
 * @format: pointer to the format
 * @i: pointer to index of format
 * Return: an integer
 */

int flag_check(const char *format, int *i)
