#include "main.h"

/**
 * size_check - entry point to check the size
 * @format: pointer to the format string
 * @i: pointer to index of format string
 * Return: an integer
 */
int size_check(const char *format, int *i)
